platform: linux 

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: latest

inputs:
  - name: ppTest

  
outputs:
  - name: resource-app
  
run:
 path: ./ppTest/ci/build.sh
 #args: ["ppTest/ci/build.sh"]